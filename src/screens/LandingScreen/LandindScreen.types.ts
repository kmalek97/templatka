import {ViewStyle, ImageStyle, TextStyle} from 'react-native';

export interface ILandingScreen {
  addressContainer: ViewStyle;
  addressText: TextStyle;
  addressTitle: TextStyle;
  body: ViewStyle;
  container: ViewStyle;
  deliveryIcon: ImageStyle;
  footer: ViewStyle;
  navigation: ViewStyle;
}
