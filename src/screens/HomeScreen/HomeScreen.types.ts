import {ViewStyle} from 'react-native';

export interface IHomeScreen {
  container: ViewStyle;
  navigation: ViewStyle;
  headerContainer: ViewStyle;
  headerSearchContainer: ViewStyle;
  body: ViewStyle;
  footer: ViewStyle;
}
